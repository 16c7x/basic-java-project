<project name="basic-java-project" default="run" basedir=".">

    <property name="src.dir"   value="src"/>
    <property name="build.dir" value="build"/>
    <property name="dist.dir"  value="dist"/>

    <target name="clean" description="Remove build artifacts">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <target name="compile" depends="clean" description="Compile source code">
        <mkdir dir="${build.dir}"/>
        <javac srcdir="${src.dir}" destdir="${build.dir}" includeantruntime="false"/>
    </target>

    <target name="jar" depends="compile" description="Package into a JAR">
        <mkdir dir="${dist.dir}"/>
        <jar destfile="${dist.dir}/basic-java-project.jar" basedir="${build.dir}">
            <manifest>
                <attribute name="Main-Class" value="HelloWorld"/>
            </manifest>
        </jar>
    </target>

    <target name="run" depends="jar" description="Run the application">
        <java jar="${dist.dir}/basic-java-project.jar" fork="true"/>
    </target>

</project>
